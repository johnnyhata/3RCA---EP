PCT=0
NOMESCRIPT[0]="ola.sh"
( 
while test $PCT != 100; do

   # Exemplo de mudanca 

de mensagem de script
   if [ $PCT -lt 50 ]; then
      MENSAGEM=$(cat telas.sh);
 

  else
      MENSAGEM=$(cat telas2.sh);
   fi
